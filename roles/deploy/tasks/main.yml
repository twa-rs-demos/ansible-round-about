---
- name: Unarchive after coping tar to remote
  unarchive:
    src: "{{ ra_archive }}"
    dest: "{{ ra_remote_dest }}"
  notify: Update service
  tags: unarchive

- include: plugins.yml tags=plugins

