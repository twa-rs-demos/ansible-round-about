---
- name: Unarchive after coping tar to remote
  unarchive:
    src: "{{ ra_archive }}"
    dest: "{{ ra_remote_dest }}"
  notify: Make sure roundabout_wordpress_1 is started
  tags: unarchive

- include: plugins.yml tags=plugins

